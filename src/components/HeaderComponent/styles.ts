import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  headerText: {
    fontSize: 25,
    color: "#FFFFFF",
    marginBottom: -9
  },
  headerComponent: {
    height: 66,
    position: "absolute",
    top: 48,
    left: 46,
    paddingLeft: 7,
    borderLeftWidth: 2,
    borderColor: "#FFFFFF"
  }
})

export { styles }
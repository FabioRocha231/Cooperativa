import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  textWrapper: {
    minHeight: 60,
    marginLeft: 52,
    borderColor: "#FFFF",
    borderLeftWidth: 3,
    paddingLeft: 6
    
  },
  text: {
    fontSize: 24, 
    color: "#FFFFFF",
    marginTop: 1
  },
  subText: {
    fontSize: 10,
    color: "#FFFFFF",
  }
})

export { styles }